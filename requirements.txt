# Core
anthropic
python-dotenv
click
rich

# Config
pyyaml

# Etsy API
requests

# 3D / Images
pillow
svgwrite
trimesh              # STL validation (watertight check, mesh repair)
numpy                # trimesh dependency

# AI / Images (optional)
openai               # DALL-E 3

# Trends (optional — graceful fallback if missing)
pytrends             # Google Trends

# Geometry (STL offset, polygon boolean ops)
shapely>=2.0.0
