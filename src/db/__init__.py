"""
src/db — warstwa bazy danych (SQLite + SQLModel).

Moduły:
    models   — definicje tabel (Product, RenderJob)
    session  — engine, get_session, init_db
    migrate  — jednorazowa migracja z flat JSON
"""
